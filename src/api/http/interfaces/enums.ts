export enum ApiTag {
  Admin = 'Admin',
  Utils = 'Utils',
  Users = 'Users',
}

/* eslint-disable no-magic-numbers */
export enum HttpStatus {
  OK = 200,
  BadRequest = 400,
  ServerError = 503,
}
/* eslint-enable no-magic-numbers */
